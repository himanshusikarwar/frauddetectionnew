# Database (SQLite for local dev; use PostgreSQL URL for production)
DATABASE_URL="file:./dev.db"

# Optional: for future real auth (e.g. NextAuth)
# NEXTAUTH_SECRET="your-secret-here"
# NEXTAUTH_URL="http://localhost:3000"
